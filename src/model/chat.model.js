class Chat {
    constructor(username, content, created_at, is_issurer) {
        this.username = username
        this.content = content
        this.created_at = created_at 
        this.is_issurer = is_issurer
    }
}

export default Chat